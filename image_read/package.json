{
  "name": "lambda-nodejs",
  "version": "1.0.0",
  "main": "optimize.js",
  "license": "MIT",
  "scripts": {
    "predeploy": "rm -rf node_modules/sharp && npm install --platform=linux --arch=x64 --target=8.10.0 sharp",
    "deploy": "serverless deploy -v",
    "posdeploy": "rm -rf node_modules/sharp && npm installl sharp"
  },
  "dependencies": {
    "sharp": "^0.29.1"
  }
}
